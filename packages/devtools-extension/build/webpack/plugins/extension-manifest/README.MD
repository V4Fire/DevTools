# Extension manifest generation plugin

This plugin creates an extension manifest file in the output directory.
By default manifest version is 3, you can change it via `--env` flag `extension-manifest-version=2`.
